# Test Fixtures

Tests should address fixtures through absolute paths derived from
`tests/conftest.py`, not through the process working directory.

Fixture data in this directory is intentionally small and local. Larger source
archives for exploratory development remain under `examples/dwca/`.
